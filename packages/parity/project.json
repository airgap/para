{
  "name": "parity",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "para/packages/parity",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun runner.ts",
        "cwd": "para/packages/parity"
      }
    },
    "canary": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun runner.ts --canary",
        "cwd": "para/packages/parity"
      }
    }
  },
  "tags": [
    "needs-parabun",
    "scope:para"
  ]
}
