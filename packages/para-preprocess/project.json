{
  "name": "para-preprocess",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/para-preprocess/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "options": { "command": "tsc", "cwd": "packages/para-preprocess" }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test test/",
        "cwd": "packages/para-preprocess"
      }
    }
  }
}
